# План: Docs — layout через `/api/data`

**Цель:** обновить документацию по layout с учетом нового формата и endpoint.
**Статус:** ⏸️ Ожидает завершения QA  
**Зависимости:** QA должен быть ✅

## Задачи
- [ ] Обновить описание layout endpoint:
  - новый запрос через `/api/data`
  - структура ответа `{ sections: [...] }`
  - где искать `formats` и `header` (внутри секций)
- [ ] Удалить/пометить устаревшее описание `/api/layout`.
- [ ] Добавить пример запроса и ответа из `LAYOUT_ENDPOINTS_COMPARISON_DETAILED.md`.

## Файлы для изменения
- `docs/api/get-data.md` или файл с описанием layout
- `docs/api/endpoints.md` (если есть упоминания `/api/layout`)

## Критерии завершения
- [ ] Документация соответствует новому контракту
- [ ] Примеры запроса и ответа актуальны

## Инструкции
1. Убедись, что QA завершен (✅).
2. Выполни задачи по порядку.
3. Обнови статус этого файла на ✅.
4. НЕ переходи к задачам других агентов.
