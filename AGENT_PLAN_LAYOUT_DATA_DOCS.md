# План: Docs — layout через `/api/data`

**Цель:** обновить документацию по layout с учетом нового формата и endpoint.
**Статус:** ✅ Завершено
**Зависимости:** QA должен быть ✅

## Задачи
- [x] Обновить описание layout endpoint:
  - новый запрос через `/api/data`
  - структура ответа `{ sections: [...] }`
  - где искать `formats` и `header` (внутри секций)
- [x] Удалить/пометить устаревшее описание `/api/layout`.
- [x] Добавить пример запроса и ответа из `LAYOUT_ENDPOINTS_COMPARISON_DETAILED.md`.

## Файлы для изменения
- `docs/api/get-data.md` или файл с описанием layout
- `docs/api/endpoints.md` (если есть упоминания `/api/layout`)
- `docs/api/layout-api.md` - основной файл документации по layout

## Критерии завершения
- [x] Документация соответствует новому контракту
- [x] Примеры запроса и ответа актуальны

## Выполненные изменения

1. **docs/api/layout-api.md:**
   - Добавлено предупреждение об устаревшем endpoint `/api/layout`
   - Добавлено описание нового endpoint через `/api/data`
   - Обновлена структура ответа с примерами для нового и старого endpoint
   - Объяснено, где искать `formats` и `header` (внутри секций)
   - Обновлены примеры TypeScript и React Hook для нового endpoint
   - Добавлены примеры извлечения `formats` и `header` из секций

2. **docs/api/get-data.md:**
   - Расширено описание специального случая `layout`
   - Добавлена структура ответа с примерами
   - Добавлены примеры извлечения данных из секций
   - Добавлена ссылка на Layout API

3. **docs/api/endpoints.md:**
   - Обновлено описание Layout endpoint
   - Добавлен новый endpoint через `/api/data` как рекомендуемый
   - Старый endpoint `/api/layout` помечен как устаревший
   - Добавлена информация о структуре ответа и извлечении данных
   - Добавлена ссылка на Layout API для деталей

## Инструкции
1. ✅ QA завершен
2. ✅ Задачи выполнены
3. ✅ Статус обновлен на ✅
