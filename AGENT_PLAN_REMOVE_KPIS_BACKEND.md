# План: Удаление `/api/kpis` — Backend

**Цель:** полностью удалить старый endpoint `/api/kpis` и его backend-зависимости.
**Статус:** ✅ Завершено  
**Зависимости:** Нет

## Задачи
- [x] Найти и удалить роут `/api/kpis` в `backend/src/routes/`.
- [x] Удалить регистрацию маршрута в `backend/src/routes/index.ts`.
- [x] Проверить и удалить сервисы/контроллеры, используемые только `/api/kpis`.
- [x] Убедиться, что `/api/data?query_id=kpis` работает.
- [x] Базовые проверки разработчика: ручной вызов `/api/data?query_id=kpis`.

## Файлы для изменения
- `backend/src/routes/kpiRoutes.ts` (если существует)
- `backend/src/routes/index.ts`
- `backend/src/services/mart/kpiService.ts` (если используется только старым endpoint)

## Критерии завершения
- [x] `/api/kpis` удалён из backend
- [x] Нет ссылок на `/api/kpis` в backend (кроме комментариев в коде)
- [x] `/api/data?query_id=kpis` работает
- [x] Базовые проверки выполнены

## Инструкции
1. Выполни задачи по порядку.
2. Обнови статус этого файла на ✅.
3. НЕ переходи к задачам других агентов.
