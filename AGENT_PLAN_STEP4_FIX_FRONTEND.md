# План: Шаг 4 (фикс) — Frontend

**Цель:** адаптировать фронт под новый формат getData.
**Статус:** ✅ Завершено

## Контекст
Было: `{ "data": [...] }`
Стало: `{ "componentId": "...", "type": "table", "rows": [...] }`

## Задачи
- [x] Обновить `useGetData` хук для работы с новым форматом
- [x] Проверить, что `DynamicTable` корректно использует `rows` из ответа
- [x] Убрать преобразование `data` → `rows` (если есть)

## Формат вызова
```
GET /api/data/assets_table?component_id=assets_table&p1=2025-08-01&p2=2025-07-01&p3=2024-08-01
```

## Файлы для изменения
- `src/hooks/useAPI.ts`
- `src/pages/DynamicDashboard.tsx` (если нужно)

## Критерии завершения
- [x] Фронт корректно парсит `{ componentId, type, rows }`
- [x] Таблица отображается
