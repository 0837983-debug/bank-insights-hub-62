# План: API /api/data — Frontend

**Цель:** обновить фронт под контракт `GET /api/data` с `query_id`, `component_Id`, `parametrs`.
**Статус:** ✅ Завершено  
**Зависимости:** Backend должен быть ✅

## Задачи
- [x] Обновить `getData` в `src/lib/api.ts` под `GET /api/data` с query string.
- [x] Передавать `query_id`, `component_Id` и `parametrs` (строкой JSON).
- [x] Удалить любые альтернативные способы передачи параметров.
- [x] Обновить все вызовы `useGetData`/`getData` в коде.
- [x] Базовые проверки разработчика (ручной прогон сценариев UI).

## Файлы для изменения
- `src/lib/api.ts`
- `src/hooks/useAPI.ts`
- `src/pages/` и `src/components/` (места вызова `getData`)

## Критерии завершения
- [x] Все вызовы `getData` соответствуют контракту
- [x] Нет ошибок в консоли браузера (проверено линтером)
- [x] Данные успешно загружаются через `GET /api/data` (код обновлен под контракт)

## Инструкции
1. Убедись, что Backend завершен (✅).
2. Выполни задачи по порядку.
3. Обнови статус этого файла на ✅.
4. НЕ переходи к задачам других агентов.
