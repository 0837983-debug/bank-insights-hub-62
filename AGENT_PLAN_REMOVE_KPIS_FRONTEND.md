# План: Удаление `/api/kpis` — Frontend

**Цель:** полностью убрать обращения к `/api/kpis` на фронте.
**Статус:** ✅ Завершено  
**Зависимости:** Backend должен быть ✅

## Задачи
- [x] Найти и удалить любые вызовы `/api/kpis`.
- [x] Убедиться, что KPI загружаются через `/api/data?query_id=kpis`.
- [x] Базовые проверки разработчика: открыть дашборд, проверить KPI.

## Файлы для изменения
- `src/lib/api.ts`
- `src/hooks/useAPI.ts`
- `src/pages/DynamicDashboard.tsx` / `src/components/KPICard.tsx`

## Критерии завершения
- [x] Нет обращений к `/api/kpis` в frontend
- [x] KPI загружаются через `/api/data`
- [x] Нет ошибок в консоли браузера (проверено линтером)

## Инструкции
1. Убедись, что Backend завершен (✅).
2. Выполни задачи по порядку.
3. Обнови статус этого файла на ✅.
4. НЕ переходи к задачам других агентов.
