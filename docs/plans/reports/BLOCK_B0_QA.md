# Отчёт QA: Блок B.0 — Balance: поддержка XLSX с Excel-датами (Этап 3)

**Дата**: 2026-01-26  
**Ответственный**: QA Agent  
**Статус**: ⚠️ Обнаружены проблемы, требующие исправления

---

## Выполненные проверки

### 1. Загрузка тестового XLSX файла

**Файл**: `/Users/pm/bin/files4upload/Balance/Средний баланс декабрь 2024 -- декабрь 2025.xlsx`

**Попытка загрузки**:
```bash
curl -X POST "http://localhost:3001/api/upload" \
  -F "file=@/Users/pm/bin/files4upload/Balance/Средний баланс декабрь 2024 -- декабрь 2025.xlsx" \
  -F "targetTable=balance" \
  -F "sheetName=Лист1"
```

**Результат**: ❌ Загрузка завершилась с ошибкой
- **Upload ID**: 43, 49
- **Статус**: `failed`
- **Ошибка**: `Отсутствуют обязательные заголовки: month, class, amount`

---

### 2. Анализ структуры файла

**Проверка структуры файла**:
- ✅ Файл успешно парсится
- ✅ Лист найден: `Лист1`
- ✅ Заголовки обнаружены: `['Class', 'Section', 'Item', 'Amount', 'Month']`
- ✅ Количество строк: 343
- ✅ **Даты корректно распознаны**: `Month: '2024-12-01'` (Excel serial date конвертирован правильно!)

**Пример данных**:
```javascript
{
  Class: 'АКТИВЫ',
  Section: 'Ликвидные активы',
  Item: 'Денежные средства',
  Amount: -3757469,
  Month: '2024-12-01'  // ✅ Дата корректно распознана!
}
```

---

### 3. Проверка mapping в БД

**Mapping для `balance`** (после обновления):
| source_field | target_field | обязательное |
|--------------|--------------|--------------|
| Month | period_date | ✅ true |
| Class | class | ✅ true |
| Section | section | ❌ false |
| Item | item | ❌ false |
| Amount | value | ✅ true |

**Обязательные поля**: `Month`, `Class`, `Amount`

---

## Обнаруженные проблемы

### ❌ Проблема 1: Жёстко закодированные заголовки в uploadRoutes.ts

**Файл**: `backend/src/routes/uploadRoutes.ts`  
**Строки**: 258-260

**Проблема**: Обязательные заголовки жёстко закодированы в нижнем регистре:
```typescript
const requiredHeaders = targetTable === "balance" 
  ? ["month", "class", "amount"]  // ❌ Жёстко закодировано!
  : [];
```

**Причина**: Даже после обновления mapping в БД на `Month`, `Class`, `Amount`, код продолжает проверять старые заголовки в нижнем регистре.

**Влияние**: Загрузка файла не проходит валидацию, данные не попадают в STG/ODS/MART.

**Решение**: 
- Получать обязательные заголовки динамически из mapping через функцию `getFieldMapping`
- Экспортировать `getFieldMapping` из `validationService.ts`
- Использовать `mapping.filter(m => m.isRequired).map(m => m.sourceField)` вместо жёстко закодированного массива

### ⚠️ Проблема 2: Case-sensitive сравнение заголовков

**Файл**: `backend/src/services/upload/fileParserService.ts`  
**Функция**: `validateFileStructure`

**Проблема**: Функция использует точное сравнение (`headers.includes(required)`), что требует точного совпадения регистра.

**Текущее состояние**: 
- Mapping обновлён на `Month`, `Class`, `Amount` (заглавные)
- Файл содержит `Month`, `Class`, `Amount` (заглавные)
- После исправления проблемы 1, валидация должна работать

**Рекомендация**: Если в будущем появятся файлы с разным регистром, можно сделать сравнение case-insensitive.

---

## ✅ Положительные результаты

### 1. Конвертация Excel Serial Date работает корректно

**Проверено**: Даты в файле корректно распознаны как `2024-12-01` (формат YYYY-MM-DD)

**Вывод**: 
- ✅ Функция `excelSerialToDate()` работает правильно
- ✅ Парсер XLSX корректно определяет Excel serial dates
- ✅ Даты конвертируются в правильный формат для БД

### 2. Парсинг XLSX файла работает

**Проверено**: 
- ✅ Файл успешно парсится
- ✅ Заголовки извлекаются корректно
- ✅ Данные читаются правильно (343 строки)
- ✅ Листы определяются (`Лист1`)

---

## Рекомендации

### Для Backend Agent:

1. **Исправить жёсткую кодировку заголовков в uploadRoutes.ts**
   - Экспортировать функцию `getFieldMapping` из `validationService.ts`
   - Заменить жёстко закодированный массив на динамическое получение из mapping:
   ```typescript
   const mapping = await getFieldMapping(targetTable);
   const requiredHeaders = mapping
     .filter((m) => m.isRequired)
     .map((m) => m.sourceField);
   ```

2. **Опционально: Сделать валидацию заголовков case-insensitive** (на будущее)
   - Обновить `validateFileStructure` для сравнения заголовков без учёта регистра
   - Или нормализовать заголовки перед валидацией (привести к нижнему регистру)

### Приоритет: Высокий

Проблема блокирует загрузку XLSX файлов Balance, хотя функциональность конвертации дат работает корректно.

---

## Проверка данных в БД

**STG (stg.balance_upload)**: Данные не загружены (загрузка не прошла валидацию)  
**ODS (ods.balance)**: Данные не загружены  
**MART (mart.balance)**: Данные не загружены

**Причина**: Загрузка не прошла валидацию из-за жёстко закодированных заголовков в коде.

---

## Выводы

### ✅ Что работает:
1. Конвертация Excel serial date → Date работает корректно
2. Парсинг XLSX файлов работает
3. Определение листов работает
4. Структура файла распознаётся правильно
5. Даты распознаются правильно (`2024-12-01`)

### ❌ Что не работает:
1. Валидация заголовков использует жёстко закодированные значения вместо mapping
2. Загрузка файла не проходит из-за несоответствия заголовков

### 📊 Статус:
- **Функциональность конвертации дат**: ✅ Работает
- **Парсинг XLSX**: ✅ Работает
- **Валидация и загрузка**: ❌ Не работает (жёстко закодированные заголовки)

---

## Следующие шаги

1. Backend Agent должен исправить жёсткую кодировку заголовков в `uploadRoutes.ts`
2. После исправления — повторная проверка загрузки файла
3. Проверка данных в STG/ODS/MART после успешной загрузки
4. Убедиться, что `period_date` содержит корректные даты

---

## Примечания

- Тестовый файл содержит корректные данные и структуру
- Проблема только в валидации заголовков (жёстко закодированные значения)
- Функциональность конвертации Excel дат реализована и работает правильно
- Mapping в БД обновлён корректно (`Month`, `Class`, `Amount`)
