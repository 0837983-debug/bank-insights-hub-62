# Product Owner Acceptance: Docker Dashboard Config Fixes

**Дата**: 2026-06-15  
**План**: `docs/plans/current/DOCKER_DASHBOARD_CONFIG_FIXES.md` — Этап 4  
**QA-отчёт**: `docs/plans/reports/DOCKER_DASHBOARD_CONFIG_FIXES_QA.md`  
**Окружение**: Docker dev stack (`http://localhost:8080`), bootstrap с миграцией 079 и 3-period fin_results seed

## Вердикт
ACCEPTED

## Проверенные сценарии
- [x] Секция **Баланс**: таблица загружается, нет ошибки wrap_json
- [x] Секция **Финансовые результаты**: таблица ФинРез с колонками и данными
- [x] KPI fin_results: цифры на p1/p2/p3 (не только прочерки)
- [x] P&L таблица (`table_pnl`): данные отображаются
- [x] Frontend доступен на `http://localhost:8080`, заголовок и периоды p1/p2/p3 понятны

## Что хорошо
- Таблица **Баланс** отображает активы и пассивы с суммами (напр. ₽13.5B / ₽11.5B) и динамикой к предыдущим периодам — без технической ошибки `wrap_json`.
- Секция **Финансовые результаты** содержит KPI-карточки с осмысленными цифрами (ЧПД ₽144M, ЧКД ₽23.3M, Чистая прибыль ₽119.2M) и процентами изменения.
- Таблица **ФинРез** (`fin_results_table`) показывает строки с данными (напр. «Операционные расходы»), колонки определены в layout (5 полей).
- Таблица **P&L** (`table_pnl`) отображает те же финансовые строки с суммами и изменениями — пользователь видит полноценный отчёт, а не пустой блок.
- В шапке явно указаны периоды: P1 (2025-02-01), P2 (2025-01-01), P3 (2024-12-01) — сравнение периодов понятно без подсказок разработчика.
- API и UI согласованы: smoke-тесты 10/10, повторная проверка PO подтвердила контракты.

## Проверка API (curl / Playwright)

| Endpoint | Результат |
|----------|-----------|
| `header_dates` | p1=2025-02-01, p2=2025-01-01, p3=2024-12-01 |
| `table_balance` | 200, 5 rows, нет wrap_json |
| `fin_results_table` | 200, 10 rows |
| `table_pnl` | 200, 10 rows |
| `layout` | `section_financial_results` → `fin_results_table` с 5 колонками, `table_pnl` с 5 колонками |
| `kpis` | 6 fin_results-карточек: все с non-null p1, p2, p3 (напр. `card_nii`: 144M / 133M / 119M) |

## Проверка UI (Playwright)

| Элемент | Результат |
|---------|-----------|
| Ошибка wrap_json на странице | Нет |
| «Колонки не определены в layout» | Нет |
| Таблицы на странице | 3 (Баланс, ФинРез, P&L) |
| KPI только прочерки | Нет (0 dash-only в фин. секции) |

## Что не устраивает

Замечаний, блокирующих приёмку, нет.

## Требования к доработке

Не требуются.

## Итог

Бизнес-цель плана достигнута: после Docker bootstrap дашборд показывает рабочие таблицы Баланс и ФинРез, KPI fin_results с данными на всех трёх периодах и P&L-таблицу. Пользователь может открыть `http://localhost:8080` и сразу увидеть заполненный дашборд без технических ошибок конфигурации. **Вердикт: ACCEPTED.**
