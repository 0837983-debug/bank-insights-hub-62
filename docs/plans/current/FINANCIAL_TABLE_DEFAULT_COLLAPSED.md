# План выполнения: FinancialTable по умолчанию свернута

> **Создан**: 2026-02-04  
> **Статус**: ✅ Завершено  
> **Roadmap**: H.5 — Типизация полей + Calculated fields

---

## Контекст

Нужно, чтобы таблицы на дашборде по умолчанию открывались **свернутыми**, как на скриншоте.  
Требуется изменить только UI‑поведение: initial state collapsed на уровне `FinancialTable` (без изменения данных).

**Файлы для изучения перед началом:**
- `docs/context/frontend.md`
- `src/components/FinancialTable.tsx`

---

## ⛔ Принципы разработки (обязательные)

### ЗАПРЕЩЕНО:
- **Fallback механизмы**
- **Хардкод имён полей**

### ОБЯЗАТЕЛЬНО:
- Один путь данных (layout)
- Чистый код без «временных» блоков

---

## Этап 1: Frontend ✅

**Субагент**: `frontend-agent`  
**Зависимости**: Нет  
**Статус**: ✅ Завершено

### Задачи:
- [x] Установить состояние collapsed по умолчанию для всех групп таблицы
- [x] Убедиться, что кнопки expand/collapse работают как сейчас
- [x] Обновить `docs/context/frontend.md`

### Файлы для изменения:
- `src/components/FinancialTable.tsx`

### Критерии завершения:
- [x] Таблица при первом открытии свернута
- [x] Expand/Collapse продолжают работать корректно
- [x] `npm run build` без ошибок
- [x] `npm run test:frontend` без ошибок (60 тестов)

### 📋 Команда для Executor (использовать Task tool!):

```javascript
Task(
  subagent_type: "frontend-agent",
  description: "FinancialTable default collapsed",
  prompt: `
    ПЕРЕД НАЧАЛОМ РАБОТЫ:
    1. Прочитай контекст: docs/context/frontend.md
    2. Прочитай план: docs/plans/current/FINANCIAL_TABLE_DEFAULT_COLLAPSED.md
    3. Редактируй ТОЛЬКО файлы указанные в плане

    ⛔ ЗАПРЕЩЕНО:
    - Хардкодить имена полей
    - Оставлять fallback

    Выполни Этап 1.

    После завершения:
    - npm run build
    - npm run test:frontend
    - Обнови docs/context/frontend.md
    - Обнови статус этапа в плане на ✅
  `
)
```

---

## Финальная проверка

```bash
npm run build
npm run test:frontend
```

---

## История выполнения

| Дата | Этап | Результат | Комментарий |
|------|------|-----------|-------------|
| 2026-02-05 | Frontend | ✅ | collapsedGroups инициализируется всеми группами, build OK, 60 тестов OK |
