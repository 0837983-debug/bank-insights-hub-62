# План выполнения: Дедупликация v_kpi_all по layout_component_mapping

> **Создан**: 2026-01-23  
> **Статус**: ⏸️ Готов к выполнению  
> **Roadmap**: `docs/plans/ROADMAP.md` — J.3

---

## Контекст

`mart.v_kpi_all` возвращает дубликаты из-за повторов в `config.layout_component_mapping`.  
Решение: дедуплицировать `layout_component_mapping` в CTE (`DISTINCT layout_id, component_id`) и JOIN делать на него.

**Файлы для изучения перед началом:**
- `docs/context/backend.md`
- `docs/context/database.md`
- `docs/plans/current/J3_KPI_LAYOUT_ID.md`

---

## Этап 1: Backend ✅

**Субагент**: `backend-agent`  
**Зависимости**: Нет  
**Статус**: ✅ Завершено

### Задачи:
- [x] Создать миграцию: пересоздать `mart.v_kpi_all` с `lcm_unique` CTE (DISTINCT layout_id, component_id).
- [x] Убедиться, что `component_id` и `layout_id` остаются в выдаче.
- [x] Проверить, что дубликаты строк исчезли (на уровне одной пары layout_id + component_id).
- [x] Обновить `docs/context/database.md` и `docs/context/backend.md`.

### Файлы изменены:
- `backend/src/migrations/058_dedup_v_kpi_all.sql` ✅
- `docs/context/database.md` ✅
- `docs/context/backend.md` ✅

### Критерии завершения:
- [x] `mart.v_kpi_all` не содержит дубликатов по `(layout_id, component_id, period_date, kpi_name)`.
- [x] Миграция проходит без ошибок (build OK).

### 📋 Команда для Executor (использовать Task tool!):

```javascript
Task(
  subagent_type: "backend-agent",
  description: "Deduplicate v_kpi_all join with layout mapping",
  prompt: `
    ПЕРЕД НАЧАЛОМ РАБОТЫ:
    1. Прочитай инструкции: .cursor/agents/backend-agent.md
    2. Прочитай контекст: docs/context/backend.md, docs/context/database.md
    3. Редактируй ТОЛЬКО файлы указанные в плане
    4. НЕ добавляй fallback/legacy логику

    Прочитай план: docs/plans/current/J3_KPI_ALL_DEDUP_LAYOUT.md, раздел "Этап 1: Backend"
    
    Выполни все задачи из раздела.
    
    После завершения:
    - Проверь компиляцию: cd backend && npm run build
    - Обнови docs/context/backend.md и docs/context/database.md
    - Обнови статус этапа в плане на ✅
  `
)
```

---

## Этап 2: QA ⏸️

**Субагент**: `qa-agent`  
**Зависимости**: Этап 1 ✅  
**Статус**: ⏸️ Ожидает

### Задачи:
- [ ] Проверить, что `v_kpi_all` не отдаёт дубликаты (ручной SQL).
- [ ] Убедиться, что KPI карточки отображаются без дублей.

### Файлы для изменения:
- `docs/plans/reports/QA_KPI_ALL_DEDUP.md` (если есть баги)

### Критерии завершения:
- [ ] Дубликаты отсутствуют.

### 📋 Команда запуска (скопировать в Executor):

```
Запусти qa-agent:
- Прочитай инструкции: .cursor/agents/qa-agent.md
- Прочитай docs/plans/current/J3_KPI_ALL_DEDUP_LAYOUT.md, раздел "Этап 2: QA"
- Проверь дубликаты в v_kpi_all
- Если есть ошибки — оформи отчёт
```

---

## Финальная проверка

После всех этапов Executor должен проверить:

```bash
# Backend компилируется
cd backend && npm run build
```

---

## История выполнения

| Дата | Этап | Результат | Комментарий |
|------|------|-----------|-------------|
| 2026-02-09 | Этап 1: Backend | ✅ | Миграция 058, дедупликация через CTE lcm_unique |
