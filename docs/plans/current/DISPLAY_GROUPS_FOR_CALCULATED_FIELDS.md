# План выполнения: UI‑переключатель групп в FinancialTable (одна кнопка)

> **Создан**: 2026-02-04  
> **Статус**: ✅ Завершён  
> **Roadmap**: H.5 — Типизация полей + Calculated fields

---

## Контекст

Переключение групп calculated полей **уже работает**, но в таблице отображаются две кнопки `% / Абс` и расположены неудачно.  
Нужно сделать **одну кнопку‑toggle** в правом верхнем углу таблицы, которая **циклически** переключает все группы, переданные в layout.  
Текст кнопки = значение `displayGroup` из layout (например, `percent`, `absolute`) — без отдельного поля‑label.

**Файлы для изучения перед началом:**
- `docs/context/frontend.md`
- `src/components/FinancialTable.tsx`

---

## Требования

- Кнопка одна, в правом верхнем углу (рядом с collapse/expand).
- Переключение цикличное по всем группам из layout **в порядке layout**.
- Текст кнопки = текущее значение `displayGroup`.
- Никаких хардкодов `%/Абс`.
- Логика групп не меняется — только UI/UX кнопки.

---

## ⛔ Принципы разработки (обязательные)

### ЗАПРЕЩЕНО:
- **Fallback механизмы**
- **Хардкод имён полей**

### ОБЯЗАТЕЛЬНО:
- Один путь данных (layout)
- Чистый код без «временных» блоков

---

## Этап 1: Frontend ✅

**Субагент**: `frontend-agent`  
**Зависимости**: Нет  
**Статус**: ✅ Завершён

### Задачи:
- [x] Заменить две кнопки `%/Абс` на **одну кнопку‑toggle**
- [x] Перенести кнопку в правый верхний угол таблицы (рядом с collapse/expand)
- [x] Реализовать циклическое переключение по `displayGroup` из layout
- [x] Показывать в кнопке текущий `displayGroup`
- [x] Обновить `docs/context/frontend.md`

### Файлы для изменения:
- `src/components/FinancialTable.tsx`

### Критерии завершения:
- [x] Одна кнопка‑toggle справа сверху
- [x] Циклическое переключение всех групп из layout
- [x] `npm run build` без ошибок
- [x] `npm run test:frontend` без ошибок

### 📋 Команда для Executor (использовать Task tool!):

```javascript
Task(
  subagent_type: "frontend-agent",
  description: "FinancialTable: single toggle cycles displayGroup",
  prompt: `
    ПЕРЕД НАЧАЛОМ РАБОТЫ:
    1. Прочитай контекст: docs/context/frontend.md
    2. Прочитай план: docs/plans/current/DISPLAY_GROUPS_FOR_CALCULATED_FIELDS.md
    3. Редактируй ТОЛЬКО файлы указанные в плане

    ⛔ ЗАПРЕЩЕНО:
    - Хардкодить имена полей
    - Оставлять fallback

    Выполни Этап 1.

    После завершения:
    - npm run build
    - npm run test:frontend
    - Обнови docs/context/frontend.md
    - Обнови статус этапа в плане на ✅
  `
)
```

---

## Финальная проверка

```bash
npm run build
npm run test:frontend
```

---

## История выполнения

| Дата | Этап | Результат | Комментарий |
|------|------|-----------|-------------|
| 2026-02-05 | Frontend | ✅ | Одна кнопка-toggle, циклическое переключение, build/tests OK |