# План выполнения: KPI‑карточки 7 в строку + меньший шрифт изменений

> **Создан**: 2026-01-23  
> **Статус**: ✅ Завершён  
> **Roadmap**: `docs/plans/ROADMAP.md` — J.3 (отображение KPI)

---

## Контекст

Нужно уместить до 7 KPI‑карточек в одну строку и немного уменьшить шрифт изменений (красный/зелёный).  
Размер карточки определяется grid‑контейнером в `DynamicDashboard.tsx`, сами карточки не имеют фиксированной ширины.

**Файлы для изучения перед началом:**
- `docs/context/frontend.md`
- `src/pages/DynamicDashboard.tsx`
- `src/components/KPICard.tsx`

---

## Этап 1: Frontend ✅

**Субагент**: `frontend-agent`  
**Зависимости**: Нет  
**Статус**: ✅ Завершено (2026-02-09)

### Задачи:
- [x] Обновить grid KPI‑карточек, чтобы на широких экранах было до 7 колонок (без динамического JS).
- [x] Уменьшить `gap` между карточками, чтобы они физически помещались.
- [x] Уменьшить шрифт текста изменений (красный/зелёный) в `KPICard`.
- [x] Обновить `docs/context/frontend.md`.
- [x] Прогнать `npm run test:frontend`.

### Файлы для изменения:
- `src/pages/DynamicDashboard.tsx`
- `src/components/KPICard.tsx`
- `docs/context/frontend.md`

### Критерии завершения:
- [ ] На широком экране (xl/2xl) видно до 7 карточек в строку.
- [ ] Шрифт изменений заметно меньше текущего.
- [ ] UI не ломается, заголовки/значения не вылезают за пределы.
- [ ] `npm run test:frontend` проходит.

### 📋 Команда для Executor (использовать Task tool!):

```javascript
Task(
  subagent_type: "frontend-agent",
  description: "Make KPI grid 7 columns and smaller change font",
  prompt: `
    ПЕРЕД НАЧАЛОМ РАБОТЫ:
    1. Прочитай контекст: docs/context/frontend.md
    2. Редактируй ТОЛЬКО файлы указанные в плане
    3. НЕ добавляй fallback/legacy логику

    Прочитай план: docs/plans/current/KPI_CARD_GRID_7_PER_ROW.md, раздел "Этап 1: Frontend"
    
    Выполни все задачи из раздела.
    
    После завершения:
    - Проверь сборку: npm run build
    - Прогони тесты: npm run test:frontend
    - Обнови docs/context/frontend.md
    - Обнови статус этапа в плане на ✅
  `
)
```

---

## Этап 2: QA ⏸️

**Субагент**: `qa-agent`  
**Зависимости**: Этап 1 ✅  
**Статус**: ⏸️ Ожидает

### Задачи:
- [ ] Проверить визуально сетку KPI‑карточек (до 7 в строку).
- [ ] Прогнать `e2e/kpi-cards-display.spec.ts`.

### Файлы для изменения:
- `docs/plans/reports/QA_KPI_CARD_GRID.md` (если есть баги)

### Критерии завершения:
- [ ] KPI‑карточки отображаются корректно в 7‑колоночной сетке.
- [ ] E2E тест проходит.

### 📋 Команда запуска (скопировать в Executor):

```
Запусти qa-agent:
- Прочитай docs/plans/current/KPI_CARD_GRID_7_PER_ROW.md, раздел "Этап 2: QA"
- Запусти: npm run test:e2e -- --reporter=list e2e/kpi-cards-display.spec.ts
- Если тест падает — оформи отчёт
```

---

## Финальная проверка

После всех этапов Executor должен проверить:

```bash
# Frontend собирается
npm run build
```

---

## История выполнения

| Дата | Этап | Результат | Комментарий |
|------|------|-----------|-------------|
| 2026-02-09 | Этап 1 Frontend | ✅ | Grid: 2→7 колонок по breakpoints, gap: 6→2, шрифт изменений: text-xs→text-[10px] |
