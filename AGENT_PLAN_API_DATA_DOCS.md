# План: API /api/data — Documentation

**Цель:** обновить документацию под новый контракт `GET /api/data`.
**Статус:** ✅ Завершено
**Зависимости:** QA должен быть ✅

## Задачи
- [x] Обновить описание `GET /api/data` в документации.
- [x] Удалить упоминания `POST /api/data`.
- [x] Добавить пример запроса с `query_id`, `component_Id`, `parametrs`.

## Файлы для изменения
- `docs/api/endpoints.md` (или актуальный файл с описанием data API)
- `docs/api/get-data.md` - детальная документация endpoint

## Критерии завершения
- [x] Документация соответствует новому контракту

## Выполненные изменения

1. **docs/api/endpoints.md:**
   - Обновлено описание `GET /api/data` (без `:query_id` в пути)
   - Указаны обязательные параметры: `query_id`, `component_Id`
   - Указан опциональный параметр: `parametrs` (JSON строка)
   - Добавлены примеры запросов с правильными параметрами
   - Добавлена информация о специальных случаях (header_dates, layout)

2. **docs/api/get-data.md:**
   - Удалены все упоминания `POST /api/data`
   - Обновлено описание endpoint на `GET /api/data` (без `:query_id`)
   - Обновлены примеры запросов с `query_id`, `component_Id`, `parametrs`
   - Обновлены TypeScript примеры под новый контракт
   - Обновлены React Hook примеры под новый контракт
   - Обновлена схема работы endpoint
   - Обновлены примеры ошибок под новый контракт

## Инструкции
1. ✅ QA завершен
2. ✅ Задачи выполнены
3. ✅ Статус обновлен на ✅
