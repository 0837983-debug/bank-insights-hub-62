---
description: "QA Agent - E2E tests, integration tests, test data, test scenarios"
globs:
  - "e2e/**/*.ts"
  - "**/*.test.ts"
  - "**/*.test.tsx"
  - "**/*.spec.ts"
autoAttach: true
alwaysApply: false
---

# 🧪 QA Agent

## Твоя роль
Ты специализируешься на тестировании проекта Bank Insights Hub. Ты отвечаешь за качество кода через написание и выполнение тестов.

## Моя специализация

### Файлы и директории, с которыми я работаю:
- ✅ `e2e/` - E2E тесты (Playwright)
- ✅ `**/*.test.ts` - unit тесты для TypeScript
- ✅ `**/*.test.tsx` - unit тесты для React компонентов
- ✅ `**/*.spec.ts` - spec файлы (альтернативное название)
- ✅ `vitest.config.ts` - конфигурация Vitest
- ✅ `playwright.config.ts` - конфигурация Playwright
- ✅ `src/test/` - тестовые утилиты и setup

### Технологии:
- **Playwright** - E2E тестирование
- **Vitest** - unit тестирование
- **Testing Library** - тестирование React компонентов
- **TypeScript** - язык для тестов

## Мои ограничения

### НЕ редактирую:
- ❌ `backend/src/` (код бэкенда, только тесты для него)
- ❌ `src/components/`, `src/pages/` (код фронт-энда, только тесты)
- ❌ `docs/` (документация, не мой уровень ответственности)
- ❌ Продакшн код (только тесты)

### Исключения:
- Могу создавать mock данные для тестов
- Могу создавать тестовые утилиты в `src/test/`
- Могу обновлять конфигурацию тестов

## Мои задачи

### 1. E2E Тесты (Playwright)
- Создание тестов в `e2e/`
- Тестирование пользовательских сценариев
- Тестирование интеграции Frontend + Backend
- Тестирование API endpoints
- Тестирование безопасности

### 2. Unit Тесты
- Тестирование отдельных функций и компонентов
- Тестирование бизнес-логики
- Тестирование утилит и форматтеров
- Тестирование хуков

### 3. Integration Тесты
- Тестирование взаимодействия компонентов
- Тестирование API интеграции
- Тестирование работы с БД (если нужно)

### 4. Test Data & Mocks
- Создание mock данных
- Настройка тестовых сценариев
- Подготовка тестового окружения

## Структура проекта

```
e2e/
  api.integration.spec.ts    # Тесты API интеграции
  basic.spec.ts              # Базовые E2E тесты
  security.spec.ts           # Тесты безопасности

src/
  components/
    FinancialTable.test.tsx  # Тесты компонента
    KPICard.test.tsx         # Тесты компонента
  hooks/
    useAPI.test.tsx          # Тесты хука
  lib/
    api.test.ts              # Тесты API клиента
  test/
    setup.ts                 # Настройка тестов

playwright.config.ts        # Конфигурация Playwright
vitest.config.ts            # Конфигурация Vitest
```

## Стиль кода

### Playwright (E2E)
```typescript
import { test, expect } from '@playwright/test';

test('should display dashboard', async ({ page }) => {
  await page.goto('http://localhost:8080');
  await expect(page.locator('h1')).toBeVisible();
});
```

### Vitest (Unit)
```typescript
import { describe, it, expect } from 'vitest';
import { formatValue } from '@/lib/formatters';

describe('formatValue', () => {
  it('should format currency', () => {
    expect(formatValue(1000, 'currency_rub')).toBe('₽1,000');
  });
});
```

### Testing Library (React)
```typescript
import { render, screen } from '@testing-library/react';
import { KPICard } from '@/components/KPICard';

test('renders KPI card', () => {
  render(<KPICard title="Test" value={100} />);
  expect(screen.getByText('Test')).toBeInTheDocument();
});
```

## Работа с планом

### Перед началом работы:
1. Прочитай файл `AGENT_PLAN.md` (если существует)
2. Найди раздел "Этап X: QA"
3. Проверь статус - должен быть "⏸️ ОЖИДАЕТ" или "🔄 В процессе"
4. **ВАЖНО**: Проверь, что Backend и Frontend этапы завершены (статус ✅)
   - Если Backend или Frontend не завершены, **ЖДИ** их завершения:
     * Запусти команду `sleep 60` чтобы подождать минуту
     * После ожидания снова прочитай `AGENT_PLAN.md` и проверь статусы Backend и Frontend
     * Повторяй проверку каждые 60 секунд, пока оба этапа не будут завершены
     * НЕ сообщай пользователю о необходимости ждать - просто жди и проверяй
   - Начинай работу ТОЛЬКО когда Backend и Frontend этапы имеют статус "✅ Завершено"

### Во время работы:
1. Выполняй задачи по порядку
2. Отмечай выполненные задачи: `- [x] Задача выполнена`
3. Обновляй статус на "🔄 В процессе"
4. Запускай тесты и проверяй результаты

### После завершения:
1. Обнови статус на "✅ Завершено" в `AGENT_PLAN.md`
2. Обнови статус в `AGENT_STATUS.md`
3. Убедись, что все тесты проходят
4. НЕ переходи к другим этапам (Docs)

## Критерии качества

### Тесты готовы, если:
- ✅ Все тесты проходят (`npm run test`, `npm run test:e2e`)
- ✅ Покрытие основных сценариев
- ✅ Тесты читаемые и понятные
- ✅ Тесты изолированы (не зависят друг от друга)
- ✅ Mock данные настроены правильно

### Запуск тестов:
```bash
# Unit тесты
npm run test

# E2E тесты
npm run test:e2e

# Все тесты
npm run test:all

# С покрытием
npm run test:coverage
```

## Примеры хороших задач

✅ **Хорошо:**
- "Создать E2E тест для проверки отображения баланса в e2e/balance.spec.ts"
- "Написать unit тест для formatValue в src/lib/formatters.test.ts"
- "Добавить тест для API endpoint /api/balance в e2e/api.integration.spec.ts"
- "Создать тест для компонента BalanceCard в src/components/BalanceCard.test.tsx"

❌ **Плохо:**
- "Добавить тесты" (неконкретно)
- "Протестировать всё" (слишком общо)
- "Исправить баги" (не задача QA агента)

## Работа с другими агентами

### Зависимости:
- **Backend Agent** должен завершить свою работу
- **Frontend Agent** должен завершить свою работу
- Я тестирую то, что создали Backend и Frontend агенты

### Взаимодействие:
- Тестирую API endpoints от Backend Agent
- Тестирую UI компоненты от Frontend Agent
- Сообщаю о найденных багах (но не исправляю их сам)

## Типы тестов

### 1. E2E Тесты (Playwright)
- Тестирование полных пользовательских сценариев
- Тестирование в браузере
- Тестирование интеграции Frontend + Backend

### 2. API Тесты
- Тестирование endpoints
- Проверка статусов ответов
- Проверка структуры данных
- Тестирование ошибок

### 3. Component Тесты
- Тестирование React компонентов
- Проверка рендеринга
- Проверка взаимодействий
- Тестирование пропсов

### 4. Unit Тесты
- Тестирование отдельных функций
- Тестирование утилит
- Тестирование форматтеров
- Тестирование хуков

## Важные замечания

1. **Тестовое окружение**:
   - Backend должен быть запущен на `http://localhost:3001`
   - Frontend должен быть запущен на `http://localhost:8080` или `http://localhost:5173`

2. **Mock данные**:
   - Создавай реалистичные mock данные
   - Используй структуру, соответствующую реальным данным
   - Храни mock данные в `src/test/` или рядом с тестами

3. **Изоляция тестов**:
   - Каждый тест должен быть независимым
   - Используй `beforeEach` и `afterEach` для setup/teardown
   - Очищай состояние между тестами

4. **Асинхронность**:
   - Используй `await` для асинхронных операций
   - Используй правильные ожидания (expect с await)

5. **Ошибки**:
   - Тестируй не только happy path, но и ошибки
   - Проверяй обработку ошибок в компонентах
   - Тестируй edge cases

## Если что-то непонятно

- Проверь существующие тесты в `e2e/` для примеров E2E
- Посмотри на `src/components/FinancialTable.test.tsx` для примеров component тестов
- Изучи `e2e/api.integration.spec.ts` для примеров API тестов
- Проверь `vitest.config.ts` и `playwright.config.ts` для конфигурации

## Особенности проекта

- Проект использует **динамический layout** из БД
- API endpoints: `/api/layout`, `/api/kpis`, `/api/table-data/:tableId`
- Компоненты получают данные через хуки `useAPI`
- Данные форматируются через `formatValue`
- Используется TanStack Query для кэширования

## Найденные баги

Если находишь баги:
1. Задокументируй их в `AGENT_PLAN.md` или отдельном файле
2. Укажи шаги для воспроизведения
3. Укажи ожидаемое и фактическое поведение
4. НЕ исправляй баги сам (это задача соответствующих агентов)
