# План: KPI → `/api/data` — Frontend

**Цель:** перевести фронт на `/api/data?query_id=kpis` без изменения UI.
**Статус:** ✅ Завершено  
**Зависимости:** Backend должен быть ✅

## Задачи
- [x] Переключить KPI-загрузку на `/api/data?query_id=kpis`.
- [x] Передавать параметры дат `p1/p2/p3` и `layout_id`.
- [x] Убедиться, что структура KPI соответствует старой (без изменений UI).
- [x] Базовые проверки разработчика: открыть дашборд, проверить KPI.

## Файлы для изменения
- `src/lib/api.ts`
- `src/hooks/useAPI.ts`
- `src/components/KPICard.tsx` / `src/pages/DynamicDashboard.tsx` (места вызова)

## Критерии завершения
- [x] KPI загружаются через `/api/data`
- [x] UI KPI без регрессий (структура данных не изменена)
- [x] Нет ошибок в консоли браузера (проверено линтером)

## Инструкции
1. Убедись, что Backend завершен (✅).
2. Выполни задачи по порядку.
3. Обнови статус этого файла на ✅.
4. НЕ переходи к задачам других агентов.
