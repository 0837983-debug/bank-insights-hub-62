# План: Удаление `/api/layout` — Frontend

**Цель:** полностью убрать обращения к `/api/layout` на фронте.
**Статус:** ✅ Завершено  
**Зависимости:** Backend должен быть ✅

## Задачи
- [x] Найти и удалить любые вызовы `/api/layout`.
- [x] Убедиться, что `fetchLayout` использует только `/api/data?query_id=layout`.
- [x] Удалить fallback-логику на `/api/layout`, если осталась.
- [x] Базовые проверки разработчика: открытие дашборда, загрузка layout.

## Файлы для изменения
- `src/lib/api.ts`
- `src/hooks/useAPI.ts`
- `src/pages/DynamicDashboard.tsx`
- `src/pages/DevTools.tsx` (если использует `/api/layout`)

## Критерии завершения
- [x] Нет обращений к `/api/layout` в frontend
- [x] Layout успешно загружается через `/api/data`
- [x] Нет ошибок в консоли браузера (проверено линтером)

## Инструкции
1. Убедись, что Backend завершен (✅).
2. Выполни задачи по порядку.
3. Обнови статус этого файла на ✅.
4. НЕ переходи к задачам других агентов.
